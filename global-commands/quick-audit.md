Run a quick claim audit (fast checks only): `python3 execution/audit_claims.py --hook`

Show me the results. Only explain FAIL or WARN items — if everything passes, just say "All clear" with the summary line.
