## Framework: Static HTML

### Directory Structure (framework-specific)
```
src/           # HTML, CSS, JavaScript source files
docs/          # Documentation pages
```

### Commands
- Dev: open `index.html` in browser (or use a local server)
- Test: `npx eslint .` (if JavaScript is used), `npx lighthouse`
