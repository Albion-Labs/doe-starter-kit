# Migration Manifest — kit v1.59.0

This manifest is consumed by `/pull-doe` pre-flight (see `directives/starter-kit-pull.md`). When a project pulls v1.59.0, the pre-flight phase greps the project for every `OLD:` phrase listed below and reports any matches as a "PULL IMPACT" warning. Behavioural changes (hook scope changes, new permission denies, etc.) are listed at the end with the grep patterns to detect at-risk project workflows.

**Pull impact summary.** v1.59.0 rewrites prohibition-style rules to positive-form instructions across the kit's directive surface. Rule meanings are preserved; only grammar changes. The two material behavioural changes are: (1) `permissions.deny` blocks `Read(.env)` at the tool layer, and (2) the `protect_directives` and `block_secrets_in_code` PreToolUse hooks gain coverage of the `bash` matcher (Tier 2 work — landing in Step 4).

## Format

Each phrase change uses three lines:

```
OLD: "<exact phrase removed from the file>"
NEW: "<phrase that replaced it>"
WHY: <one-line reason — usually positive form name, or load-bearing → decoration deletion>
```

Entries sit under `### <file path>` headings, grouped by tier. Behavioural changes use a different shape (see end). The manifest is consumed line-by-line by `/pull-doe`, so the OLD: / NEW: prefix matters for parsing.

## Tier 1 — universal CLAUDE.md template + Core Behaviours

### CLAUDE.md (kit's own — also the project-CLAUDE.md template)

OLD: "IMPORTANT: Never do execution inline when a script exists. Check `execution/` first."
NEW: "IMPORTANT: Check `execution/` first; reuse existing scripts whenever they cover the task."
WHY: positive form names the action (reuse existing scripts).

OLD: "**Ask, don't assume.** Ambiguous? Ask. Separate research and implementation sessions."
NEW: "**Ask when ambiguous.** Match the question to the smallest decision that unblocks you. Separate research from implementation sessions."
WHY: positive form names the trigger (when ambiguous) and the action (smallest unblocking question).

OLD: "**Explain simply.** No jargon without context. Trade-offs in terms the user can evaluate."
NEW: "**Explain simply.** Use plain language; introduce jargon after defining it. Frame trade-offs in terms the user can evaluate."
WHY: positive form names the action (use plain language; define before using jargon).

OLD: "**Directive** (`directives/`): Markdown SOPs -- goals, inputs, tools, outputs, edge cases. No code."
NEW: "**Directive** (`directives/`): Markdown SOPs -- goals, inputs, tools, outputs, edge cases. Pure prose."
WHY: decoration → positive descriptor (pure prose) replaces negation (no code).

OLD: "Never commit `.env` files -- credentials must stay local. The pre-commit hook blocks this, but only works if `git config core.hooksPath .githooks` has been run."
NEW: "`.env` files stay local; the pre-commit hook gates accidental commits, provided `git config core.hooksPath .githooks` has been run on this clone."
WHY: positive form ("stay local"), and the hook is now the gate-mechanism not the prohibition.

OLD: "Always re-read the triggers relevant to your current task after a `/clear` or long conversation."
NEW: "Re-read the triggers relevant to your current task after a `/clear` or long conversation."
WHY: drop "Always" — paired NEVER/ALWAYS pattern is a Pink Elephant signal; positive form needs no intensifier.

OLD: "Never add randomness, API calls, or interactive prompts to them. AI reasoning belongs in orchestration (CLAUDE.md + directives), not execution."
NEW: "Keep randomness, API calls, and interactive prompts out of `execution/` -- AI reasoning lives in orchestration (CLAUDE.md + directives)."
WHY: positive form ("keep out of execution"), and the location of AI reasoning is now stated positively (lives in orchestration).

### universal-claude-md-template.md

OLD: "Never use emojis inside bordered ASCII boxes — use text-only labels."
NEW: "Bordered output uses text-only labels; emojis render double-width in terminal monospace fonts and break border alignment."
WHY: positive form names the convention (text-only labels) and explains why (double-width + alignment break).

OLD: "Don't use `$$` for cross-call file naming — use a fixed name instead."
NEW: "Use a fixed file name for cross-call state in Claude Code Bash tool calls; `$$` is the subshell PID and differs between calls."
WHY: positive form names the action (fixed file name) and explains the PID quirk descriptively.

OLD: "Always write to a single fixed-name file and overwrite — hooks only need the latest state, not history."
NEW: "Use a single fixed-name file and overwrite each invocation; hooks need the latest state, not history."
WHY: drop "Always" intensifier; positive form ("Use ... and overwrite").

OLD: "Multi-section formatted output (wrap-ups, reports, dashboards) must be assembled and printed as a single block — never fragment across multiple tool calls."
NEW: "Assemble multi-section formatted output (wrap-ups, reports, dashboards) as a single block, generated in one script and printed in one tool call."
WHY: positive form names the action ("Assemble ... as a single block, ... in one tool call").

OLD: "Tool output alone is not sufficient — the user expects it as your actual reply. Never summarise what was already generated; echo it in full."
NEW: "After generating formatted output, echo the full output as text in your response. Tool output alone is invisible to the user; the user expects the output as your actual reply."
WHY: positive form ("echo the full output as text"); the descriptive negation about tool-output-invisibility stays as descriptive context.

OLD: "Never commit directly to `~/doe-starter-kit` during feature work — not even 'as you go.' Make all changes in the project repo first, then run `/sync-doe` once at the end."
NEW: "During feature work, make changes in the project repo first; run `/sync-doe` once at feature end to push them to the kit."
WHY: positive form names the workflow (project repo first, sync at end). The risk of direct commits stays in subsequent sentences as descriptive context.

OLD: "Don't skip this even for 'obvious' edits."
NEW: "Verify even 'obvious' edits."
WHY: positive form names the action.

### Additive (no OLD)

ADDED: `## Grammar` section explaining the positive-form convention and citing the Pink Elephant evidence (arxiv 2503.22395).
ADDED: `## Canary` section with three leading-indicator rules (British English / cite directives by file path / Unicode borders + ASCII inside).

## Tier 2 — directives/

**Procedure:** as Step 4 rewrites each directive, append a block under the
matching `### directives/...` heading below. Each block uses the OLD: /
NEW: / WHY: format from the Format section above. Files marked "clean"
in the audit doc keep their stub heading with no entries — the empty
heading is the audit trail. Step 4's contract requires at least 20 of
these directive headings to have at least one OLD/NEW entry.

### directives/_TEMPLATE.md


(Clean per audit — no negation patterns.)

### directives/adversarial-review/README.md

OLD: "| High: shared files, API changes, data model, security | **FULL** (3-agent: Finder + Adversarial + Referee) | Cannot opt down |"
NEW: "| High: shared files, API changes, data model, security | **FULL** (3-agent: Finder + Adversarial + Referee) | MANDATORY at this severity |"
WHY: positive form names the requirement (mandatory) instead of the prohibition (cannot opt down)

OLD: "Run `claude --agent=Finder` in a separate terminal. Tool restrictions are **partially mechanical** -- `tools: Read, Grep, Glob, Bash` in the agent file means the agent cannot call Edit or Write (mechanically blocked by Claude Code). Bash is included (agents need `git log`, `ls`, etc.) but is **prompt-restricted** -- agents are instructed \"you are read-only\" and \"you must NOT edit or write any files\". This is high-confidence but not a hard platform guarantee for Bash specifically."
NEW: "Run `claude --agent=Finder` in a separate terminal. Tool restrictions are **partially mechanical** -- `tools: Read, Grep, Glob, Bash` in the agent file means only those four tools are available (Edit and Write are mechanically absent from the agent's toolbox). Bash is included (agents need `git log`, `ls`, etc.) but is **prompt-restricted** -- agents are instructed to operate read-only: their job is to inspect code and report findings, with no file writes via Bash redirections. This is high-confidence but not a hard platform guarantee for Bash specifically."
WHY: positive form names the toolbox (only those four tools available; agent inspects + reports) instead of the prohibitions

### directives/adversarial-review/code-quality-reviewer.md

OLD: "Evaluate the implementation across the categories below. For each issue, cite the exact file path and line number. Do not raise issues without evidence."
NEW: "Evaluate the implementation across the categories below. Every issue cites the exact file path and line number -- evidence is the price of raising the issue."
WHY: positive form names the rule (every issue cites + evidence is the price)

OLD: "Do not use any other severity labels."
NEW: "Severity labels are exactly: Critical / Important / Minor."
WHY: positive form names the allowed labels (the closed set)

### directives/adversarial-review/implementer-prompt.md

OLD: "**Stop. Read this gate before touching any file.**\n\n1. Re-read the contract criteria above. Do you understand what \"done\" looks like for each criterion?\n2. Check `execution/` for existing scripts that cover this task. Do not duplicate logic that already exists.\n3. Read `learnings.md` for any additional patterns relevant to the files or APIs you will touch.\n4. If anything is ambiguous — a criterion you cannot interpret, a file dependency you cannot resolve, a behaviour that is underspecified — **raise it now, before starting work**. List each question clearly. Do not proceed until you have answers or can make a safe assumption you state explicitly.\n\nDo not begin implementation until this gate is satisfied."
NEW: "**Begin implementation only after this gate is satisfied.**\n\n1. Re-read the contract criteria above. Confirm you understand what \"done\" looks like for each criterion.\n2. Check `execution/` for existing scripts that cover this task. Reuse what's there before writing new logic.\n3. Read `learnings.md` for any additional patterns relevant to the files or APIs you will touch.\n4. When anything is ambiguous — a criterion you cannot interpret, a file dependency you cannot resolve, a behaviour that is underspecified — **raise it now, before starting work**. List each question clearly. Wait for answers, or make a safe assumption you state explicitly, before any code change."
WHY: positive form leads with the precondition list (begin only after gate satisfied; reuse + state assumptions)

OLD: "If at any point you hit a blocker — a missing dependency, an architectural constraint you cannot resolve, a test that cannot be made to pass — stop immediately and report it.\n\n**It is always OK to stop and say \"this is too hard for me.\" Bad work is worse than no work.**\n\nDo not work around a blocker silently. Do not produce half-complete output without flagging it. A blocked report with a clear explanation is more useful than a completed report that hides problems."
NEW: "When you hit a blocker — a missing dependency, an architectural constraint you cannot resolve, a test that cannot be made to pass — pause and report it.\n\n**Reporting \"this is too hard for me\" is a legitimate output. A flagged failure is more useful than a hidden one.**\n\nReport blockers and partial output explicitly. A blocked report with a clear explanation is more useful than a completed report that hides problems."
WHY: positive form names the action (pause + report) and the rule (a flagged failure is more useful than a hidden one)

OLD: "- **Surgical edits only.** Edit only the code that needs to change. Do not rewrite entire files to fix small problems."
NEW: "- **Surgical edits only.** Edit only the lines that change; surgical diffs only. Wholesale rewrites require user approval first."
WHY: positive form names the rule (edit only changing lines; surgical diffs)

OLD: "- **Check before creating.** Before creating any new file, verify no similar file exists. Never create `filename-v2`, `filename-new`, or any variant. Edit the existing file."
NEW: "- **Check before creating.** Before creating any new file, verify a similar file does not already exist; if one does, edit it. New variants (`filename-v2`, `filename-new`) require explicit user approval."
WHY: positive form names the action (check + edit) and the gate for variants

OLD: "- **Follow the directory structure.** Execution scripts go in `execution/`. Do not place files in the project root or invent new directories."
NEW: "- **Follow the directory structure.** Execution scripts go in `execution/`, plans in `.claude/plans/`, visual docs in `docs/`. New directories or root-level files require explicit user approval."
WHY: positive form names the locations and the gate for new directories

OLD: "- **No secrets in code.** Credentials go in `.env` only — never in source files, comments, or logs."
NEW: "- **No secrets in code.** Credentials live in `.env` only -- source files, comments, and logs reference them via the documented loader, not as literals."
WHY: positive form names the rule (.env + documented loader)

OLD: "- **Verify after editing.** After creating or editing files, run `ls` or read the file back to confirm the change landed. Do not report success without checking."
NEW: "- **Verify after editing.** After creating or editing files, run `ls` or read the file back to confirm the change landed. Reporting success requires the verification output."
WHY: positive form names the rule (success requires verification output)

### directives/adversarial-review/spec-reviewer.md

OLD: "Check the implementation against the contract criteria and plan. Do not trust the implementer's self-assessment — verify independently."
NEW: "Check the implementation against the contract criteria and plan. Verify each contract criterion independently against the code and the `Verify:` output -- the implementer's self-assessment is one input among many, not the truth."
WHY: positive form names the action (verify each criterion independently) and the relationship to self-assessment

OLD: "## Rules\n\n- **DO NOT praise.** Do not comment on code quality, style, or elegance. That is not your job.\n- **DO NOT suggest improvements.** Do not recommend refactors, better patterns, or enhancements. Only verify spec compliance.\n- **DO NOT speculate.** If you cannot determine whether a criterion is satisfied from the code and verify output, say so — do not guess.\n- Cite evidence for every finding. Unsupported assertions are not useful."
NEW: "## Rules\n\n- **Job:** verify each contract criterion as PASS / FAIL / UNVERIFIABLE. Out of scope: code quality commentary, refactor suggestions, enhancement ideas.\n- **When a criterion cannot be determined** from the code and `Verify:` output, mark it UNVERIFIABLE with the specific reason -- guessing is worse than naming the gap.\n- **Cite evidence for every finding.** Unsupported assertions are not useful."
WHY: positive form names the job (PASS/FAIL/UNVERIFIABLE) and the out-of-scope list as a closed set

OLD: "If a category has no findings, omit it entirely. Do not write \"None\" under a category heading — just skip the section."
NEW: "If a category has no findings, omit the category heading entirely."
WHY: positive form names the rule (omit the heading) instead of the prohibition (don't write None)

### directives/architectural-invariants.md

OLD: "## Goal\nDefine truths that must survive any refactor, migration, or feature addition. These are non-negotiable constraints -- if a change would violate one, stop and flag it before proceeding."
NEW: "## Goal\nDefine truths that must survive any refactor, migration, or feature addition. These are constitutional constraints -- when a change would violate one, surface the conflict to the user before any edit."
WHY: positive form names the action (surface the conflict) instead of the prohibition (stop)

OLD: "**STATE.md reflects current position, not history.** Replace stale info, don't accumulate. History lives in git log and archive.md."
NEW: "**STATE.md reflects current position, not history.** Replace stale info as it changes; history lives in git log and archive.md."
WHY: decoration: drop redundant negation, keep positive instruction

OLD: "10. **New invariants require user approval.** Claude can propose additions to this list but must not add them unilaterally. These are constitutional constraints, not suggestions."
NEW: "10. **New invariants require user approval.** Claude proposes additions; the user merges them. These are constitutional constraints, not suggestions."
WHY: positive form names the workflow (propose; user merges)

OLD: "When a proposed change would violate an invariant:\n1. Stop before making the change"
NEW: "When a proposed change would violate an invariant:\n1. Surface the conflict before any edit"
WHY: positive form names the action (surface) instead of the prohibition (stop)

### directives/best-practices/html-css.md

OLD: "- Never use `innerHTML` with untrusted or dynamic data — use `textContent` or DOM APIs (XSS risk)"
NEW: "- For untrusted or dynamic data, use `textContent` or DOM creation APIs (`document.createElement`); reserve `innerHTML` for known-safe HTML you generated yourself (XSS risk)"
WHY: positive form names the safe API (textContent, createElement) and the narrow innerHTML use

OLD: "- Never include inline event handlers (`onclick=\"...\"`) with dynamic values"
NEW: "- Wire event handlers via `addEventListener` in JS rather than inline `onclick=\"...\"` attributes -- inline handlers with dynamic values are a code-injection vector"
WHY: positive form names the safe API (addEventListener) and the why

OLD: "- Never reuse `id` values on a page — IDs must be unique (JS selectors and anchors break)"
NEW: "- Each `id` is unique per page; reach for classes when you need a repeated marker (JS selectors and anchor links rely on uniqueness)"
WHY: positive form names the rule (unique per page) and the alternative (classes)

OLD: "- Never use inline styles — use CSS classes (maintenance, caching, consistency)"
NEW: "- Style via CSS classes -- the cascade lives in stylesheets where it can be maintained, cached, and kept consistent"
WHY: positive form names the rule (style via classes) and the why

OLD: "- Never use `!important` unless overriding third-party CSS — fix specificity instead"
NEW: "- Resolve specificity issues by tightening selectors or restructuring the cascade; reserve `!important` for overriding third-party CSS"
WHY: positive form names the action (tighten selectors) and the narrow `!important` use

OLD: "- Use CSS custom properties (`--color-primary`) for repeated values — never hardcode colors/sizes in multiple places"
NEW: "- Use CSS custom properties (`--color-primary`) for repeated values -- a single source of truth for colours and sizes"
WHY: decoration: drop the never-hardcode clause; the positive instruction is the custom-property pattern

OLD: "- Never use fixed pixel widths for layout containers — use relative units, flexbox, or grid"
NEW: "- Layout containers use relative units (`%`, `rem`, `vw`), flexbox, or grid -- fixed pixel widths break responsive layouts"
WHY: positive form names the safe units/APIs (relative, flexbox, grid)

### directives/best-practices/javascript.md

OLD: "- Never use `innerHTML` with untrusted data — use `textContent` or DOM APIs (XSS risk)"
NEW: "- For untrusted data, use `textContent` or DOM creation APIs (`document.createElement`); reserve `innerHTML` for known-safe HTML you generated yourself (XSS risk)"
WHY: positive form names the safe API (textContent, createElement)

OLD: "- Never build HTML with string concatenation — use template literals with proper escaping or DOM creation"
NEW: "- Build HTML via DOM creation APIs or template literals with explicit escaping -- string concatenation hides escape boundaries and invites XSS"
WHY: positive form names the safe APIs and the why

OLD: "- Never use `eval()` or `new Function()` with dynamic input"
NEW: "- For dynamic execution, use pre-registered, statically-analysable code paths (`eval()` and `new Function()` are off-limits, including via `setTimeout`/`setInterval` with string arguments)"
WHY: positive form names the rule (statically-analysable paths) and the off-limits APIs as a closed set

OLD: "- Never mutate function parameters — return new values instead"
NEW: "- Treat function parameters as read-only; return new values to convey changes"
WHY: positive form names the rule (parameters read-only; return new values)

OLD: "- Always use `const` by default, `let` when reassignment is needed — never `var`"
NEW: "- Use `const` by default, `let` when reassignment is needed; `var` is reserved for legacy code you cannot edit"
WHY: positive form names the rule (const/let by default; var only for legacy)

OLD: "- Never hardcode magic numbers or strings — extract to named constants or config"
NEW: "- Extract magic numbers and strings to named constants or config -- a name documents intent that the literal value can't"
WHY: positive form names the action (extract to constants/config) and the why

OLD: "- Never use `async` on a function that doesn't `await` anything"
NEW: "- Mark a function `async` only when it awaits something -- otherwise the wrapped Promise is overhead with no caller benefit"
WHY: positive form names the rule (async only when awaiting) and the why

### directives/best-practices/python.md

OLD: "- Never use `os.system()` or `subprocess` with `shell=True` — use `subprocess.run()` with a list of args (command injection risk)"
NEW: "- Use `subprocess.run([...])` with a list of args (no `shell=True`, no `os.system()`) -- the list form passes args without shell parsing, eliminating command-injection vectors"
WHY: positive form names the safe API (subprocess.run with list args) and the why

OLD: "- Never use f-strings or `.format()` to build SQL queries — use parameterised queries"
NEW: "- Build SQL queries with parameter bindings (`cursor.execute(\"... WHERE id = %s\", (uid,))` or the ORM's parameterised API) -- f-strings and `.format()` interleave user input with the query, which is SQL injection"
WHY: positive form names the safe API (parameter bindings) and the why

OLD: "- Never use f-strings to build shell commands — use `subprocess.run([...])` with separate args"
NEW: "- Build shell commands with `subprocess.run([...])` and separate args -- f-strings into shell strings is command injection in waiting"
WHY: positive form names the safe API (subprocess.run with list args)

OLD: "- Never use `import *` — import specific names (hides dependencies, pollutes namespace)"
NEW: "- Import specific names (`from module import foo, bar`) -- explicit imports document dependencies and keep the namespace clean"
WHY: positive form names the rule (explicit imports) and the why

OLD: "- Never use mutable default arguments (`def f(items=[])`) — use `None` and assign inside the function"
NEW: "- Default mutable arguments are a Python footgun: use `def f(items=None)` and assign `items = items or []` inside the function -- the default expression evaluates once at definition time, so a shared list/dict accumulates across calls"
WHY: positive form names the safe pattern (None default + inside-function assign) and the why

OLD: "- Always catch specific exceptions — never bare `except:` or `except Exception:` (swallows real errors)"
NEW: "- Catch specific exception classes (`except FileNotFoundError`, `except json.JSONDecodeError`) -- bare `except:` and `except Exception:` swallow real errors and mask bugs"
WHY: positive form names the rule (specific exceptions) and the why

OLD: "- Always use `with` statements for file handles, DB connections, locks — never manual open/close"
NEW: "- Use `with` statements for file handles, DB connections, and locks -- the context manager guarantees cleanup even on exceptions"
WHY: positive form names the rule (with statements) and the why

OLD: "- Always use `Path` for path manipulation — never `os.path.join()` or string concatenation"
NEW: "- Use `pathlib.Path` for path manipulation -- the `/` operator is platform-aware and the API is safer than string concatenation or `os.path.join()`"
WHY: positive form names the safe API (pathlib.Path) and the why

OLD: "- Never hardcode file paths, URLs, or credentials — use config, environment variables, or constants"
NEW: "- Source paths, URLs, and credentials from config files, environment variables, or named module-level constants -- inline literals drift across edits and leak secrets into source"
WHY: positive form names the source (config/env/constants) and the why

OLD: "- Never silence errors with `pass` in except blocks — at minimum log the error"
NEW: "- When an error is intentionally non-fatal, log it (at minimum `logger.exception(...)`) before continuing -- silent `pass` in an except block hides the diagnostic"
WHY: positive form names the action (log it) and the rule (silent pass = lost diagnostic)

### directives/best-practices/react.md

OLD: "- Never mutate state directly — always use the setter from `useState` or return new objects/arrays"
NEW: "- Apply state changes via the setter from `useState` or by returning new objects/arrays from a reducer -- the mutation/replacement distinction is what triggers React's re-render"
WHY: positive form names the safe API (setter / new objects) and the why

OLD: "- Always provide a stable, unique `key` prop in lists — never use array index as key for lists that reorder, filter, or grow"
NEW: "- Provide a stable, unique `key` prop in lists -- prefer a record id; array index is only safe when the list is read-only and never reordered"
WHY: positive form names the rule (stable id) and the narrow exception (read-only list)

OLD: "- Never use `useEffect` to compute derived state — calculate it during render instead (simpler, fewer re-renders)"
NEW: "- Compute derived state during render (or in `useMemo` for expensive cases) -- `useEffect` for derivation costs an extra render and risks stale closures"
WHY: positive form names the safe APIs (render / useMemo) and the cost of the wrong choice

OLD: "- Never call hooks conditionally or inside loops — hooks must always be at the top level of the component"
NEW: "- Call hooks at the top level of the component, in the same order on every render -- React tracks hook state by call order, so conditional or looped hooks break that mapping"
WHY: positive form names the rule (top level + same order) and the why (call-order tracking)

OLD: "- Never define functions inline in JSX props when the child component uses `React.memo` — extract to `useCallback` or define outside render"
NEW: "- For `React.memo`'d children, extract callback props to `useCallback` or define them outside render -- inline arrow functions create a new identity each render, defeating memoisation"
WHY: positive form names the safe APIs (useCallback / outside render) and the why

OLD: "- Never create new objects/arrays in JSX props — extract to `useMemo` or define outside render"
NEW: "- For object/array props passed to memoised children, extract to `useMemo` or define outside render -- a fresh object each render breaks reference equality"
WHY: positive form names the safe APIs (useMemo / outside render) and the why

OLD: "- Never prop-drill through 3+ levels — use Context, composition (`children`), or state management"
NEW: "- For data needed 3+ levels deep, use Context, component composition (`children`), or external state management -- prop-drilling couples intermediate components to data they don't read"
WHY: positive form names the safe APIs (Context, composition, state management) and the why

OLD: "- Keep side effects in `useEffect` or event handlers — never in the render body"
NEW: "- Side effects belong in `useEffect` or event handlers -- the render body must be pure so React can re-run it without observable consequences"
WHY: positive form names the rule (effects in useEffect/handlers) and the why (pure render body)

OLD: "- Never mix data fetching with presentation — separate container/data components from display components"
NEW: "- Separate data-fetching components from display components -- the data layer holds loading/error/state machinery, the display layer renders props"
WHY: positive form names the rule (separate layers) and what each layer does

OLD: "- Never use `dangerouslySetInnerHTML` without explicit sanitisation"
NEW: "- When `dangerouslySetInnerHTML` is unavoidable (Markdown render, sanitised CMS payload), pass through DOMPurify or an equivalent allowlist sanitiser first -- the API name is a warning sign for unscoped use"
WHY: positive form names the safe pattern (DOMPurify allowlist) and the narrow allowed use

### directives/best-practices/tdd-and-debugging.md

OLD: "One cycle per behaviour. Don't batch. Don't write three tests then implement all three — that's batch-and-pray, not TDD."
NEW: "One cycle per behaviour. Implement after the test fails -- batching three tests then implementing all three is batch-and-pray, not TDD."
WHY: positive form names the rule (one cycle, implement after fail)

OLD: "**Rule:** If you catch yourself using any of these excuses, stop and write the test. The excuse is the signal that a test is needed."
NEW: "**Rule:** When you notice an excuse from the table above, the excuse is the signal -- write the test now."
WHY: positive form names the action (write the test now) and the rule (excuse = signal)

OLD: "When something fails, follow these four phases in order. Do not skip phases."
NEW: "When something fails, run the four phases in order: investigate -> pattern analysis -> hypothesis testing -> implementation. Each phase produces evidence that the next one needs."
WHY: positive form names the order (with the phase names inline) and the why (each phase feeds the next)

OLD: "- Read the relevant source code. Don't guess what a function does — read it."
NEW: "- Read the relevant source code. Read the function before forming a hypothesis -- the source is the ground truth."
WHY: positive form names the rule (read source first) and the why (ground truth)

OLD: "- One hypothesis at a time. Don't change three things and see if it works."
NEW: "- One hypothesis at a time -- change one thing, test it, revert if rejected, then try the next."
WHY: positive form names the workflow (change one + test + revert)

OLD: "- Never say \"I think the problem might be...\" without immediately stating how to test that theory."
NEW: "- Each hypothesis pairs with a test that distinguishes it from the alternatives -- \"I think the problem might be...\" is incomplete without the test plan that follows."
WHY: positive form names the rule (hypothesis + distinguishing test) and what's missing without it

### directives/break-glass.md

OLD: "**Step 1: STOP.** Do not run more commands. Do not try to \"quickly fix it.\" Stop and assess. Stay calm. Nothing is unfixable."
NEW: "**Step 1: HALT.** Run only `git status` and other read-only inspection commands. Assess before any write. Stay calm -- nothing is unfixable."
WHY: positive form names the allowed actions (read-only inspection) instead of stacking prohibitions

OLD: "**Step 3: Show the user, get permission.** Never fix silently. Present the relevant options:"
NEW: "**Step 3: Present options, get permission.** Show the user the relevant options and wait for explicit approval before any remediation:"
WHY: positive form names the workflow (present + wait) and folds in the prohibition descriptively

OLD: "Explain each option in plain language. Wait for the user to say \"go ahead.\" Do not proceed without explicit permission."
NEW: "Explain each option in plain language. Wait for explicit user approval (\"go ahead\" or equivalent) before any write."
WHY: positive form names the gate (explicit approval before any write)

OLD: "3. If a script caused the problem, fix the script so it cannot happen again"
NEW: "3. If a script caused the problem, fix the script in the same session -- close the loop before moving on"
WHY: decoration: replace the can't-happen-again clause with a positive close-the-loop instruction

### directives/building-rules.md

OLD: "- Commit after every completed step. Push immediately. Never batch multiple steps."
NEW: "- Commit after every completed step. Push immediately. One commit per step keeps each change independently revertable."
WHY: positive form names the property gained (independently revertable)

OLD: "- Do not commit directly to main. No \"Co-authored-by\" trailers."
NEW: "- Commit on feature branches; merge to main via PR. Commit messages omit \"Co-authored-by\" trailers (the commit-msg hook strips them)."
WHY: positive form names the workflow (branches + PR) and the hook behaviour

OLD: "- **Check before creating.** Check if a similar file exists. Never create `filename-new`, `_v2`, `-copy` variants. Edit the existing file."
NEW: "- **Check before creating.** Check if a similar file exists and edit it. New variants (`filename-new`, `_v2`, `-copy`) require explicit user approval."
WHY: positive form names the action (check + edit) and the gate (explicit approval) for variants

OLD: "- **Surgical edits only.** When fixing a bug, edit only the affected code. Never rewrite an entire file to fix a small problem. If a rewrite is genuinely needed, say so and get approval first."
NEW: "- **Surgical edits only.** Edit only the lines that change. Wholesale rewrites require user approval first -- say so and wait."
WHY: positive form names the action (edit only the lines that change) and the gate (approval first)

OLD: "- **One task, one session.** If the conversation drifts, recommend `/clear`. Do not let unrelated context accumulate."
NEW: "- **One task, one session.** When the conversation drifts, recommend `/clear` -- keep context scoped to the active task."
WHY: decoration: drop the negation, the positive instruction is `/clear`

OLD: "- **Refactor is not rewrite.** Change structure only. Do not change behaviour. If behaviour must change, say so explicitly."
NEW: "- **Refactor is not rewrite.** Refactor preserves behaviour. Behaviour changes are tracked as feature work -- say so explicitly when a refactor will alter observable behaviour."
WHY: positive form states the property (refactor preserves behaviour) and the escalation path

OLD: "- **Plans go in `.claude/plans/`, not global.** Never write to `~/.claude/plans/`."
NEW: "- **Plans go in `.claude/plans/`.** Project plans live in the project repo; `~/.claude/plans/` is the personal sandbox and stays out of project commits."
WHY: positive form names the locations (project plans here, sandbox there)

OLD: "- **Visual docs go in `docs/`, not global.** Never write to `~/.agent/diagrams/`."
NEW: "- **Visual docs go in `docs/`.** Project diagrams live in the project repo; `~/.agent/diagrams/` is the personal sandbox."
WHY: positive form names the locations (project diagrams here, sandbox there)

OLD: "- **Files in designated directories.** Follow the directory structure in CLAUDE.md. Do not create files in the project root or invent new directories without approval."
NEW: "- **Files in designated directories.** Follow the directory structure in CLAUDE.md. New directories or root-level files require explicit approval."
WHY: positive form names the gate (explicit approval) for new directories or root files

### directives/chrome-verification.md

OLD: "- Do NOT auto-enable Chrome (it increases context usage). Just prompt."
NEW: "- Prompt the user to enable Chrome rather than auto-enabling -- auto-enable would inflate context usage even on sessions that won't use it."
WHY: positive form names the action (prompt) and explains the cost

OLD: "- Do NOT start a server just for this — only use if one is already running"
NEW: "- Use an already-running server. When none is running, ask the user before starting one (no auto-start for opportunistic verification)."
WHY: positive form names the action (use existing) and the escalation when missing

OLD: "- \"Layout looks correct\" → Screenshot and describe what's visible, but don't judge aesthetic quality"
NEW: "- \"Layout looks correct\" → Screenshot and describe what's visible; aesthetic judgement is the user's call"
WHY: positive form names whose call it is (the user's), drops the prohibition

OLD: "- **Port conflict:** If port 8080 is occupied by something else, snagging orchestrator will detect this. Don't try to start a second server."
NEW: "- **Port conflict:** When port 8080 is occupied, the snagging orchestrator surfaces the conflict -- stop and ask the user to free the port."
WHY: positive form names the action (surface + ask) instead of the prohibition (don't start a second)

### directives/claim-auditing.md

OLD: "3. For each FAIL: fix the underlying issue, not the symptom. If a doc is stale, update the doc — don't just bump the version."
NEW: "3. For each FAIL: fix the underlying issue. A stale doc gets its content updated before any version bump -- the version follows the truth, not the other way round."
WHY: positive form names the workflow (fix issue → version follows) and removes the don't

OLD: "4. For each WARN: assess whether action is needed. WARNs don't block commits but may indicate drift."
NEW: "4. For each WARN: assess whether action is needed. WARNs are advisory (commits still pass) and often signal drift worth investigating."
WHY: positive form names the property (advisory) and the value (drift signal)

### directives/context-management.md

OLD: "1. Treat ALL directives as unloaded -- do not assume you remember their contents"
NEW: "1. Treat ALL directives as unloaded -- re-read each one before acting on its rules"
WHY: positive form names the action (re-read) instead of the prohibition (don't assume)

OLD: "Use Neutral Prompts when evaluating your own work. Never self-congratulate (\"this looks great!\") or assume success. Instead: state what you did, state what you verified, state what remains. Let the verification results speak."
NEW: "Use Neutral Prompts when evaluating your own work. State what you did, state what you verified, state what remains -- let the verification results speak. Self-congratulation (\"this looks great!\") is a signal that the verification step was skipped."
WHY: positive form leads with the action (state what you did/verified/remain) and folds the warning in as a signal

OLD: "**One task, one session:** Don't let conversations drift. If the user pivots to an unrelated topic, recommend `/clear` before continuing. Unrelated context is dead weight."
NEW: "**One task, one session:** Recommend `/clear` when the user pivots to an unrelated topic. Unrelated context is dead weight -- a fresh session keeps reasoning crisp."
WHY: positive form names the action (recommend `/clear` on pivot)

OLD: "**Shared-file awareness:** The only coordination rule: don't edit STATE.md, learnings.md, todo.md, or CLAUDE.md from multiple terminals simultaneously. If you need to update a shared file, check whether another terminal might be mid-edit."
NEW: "**Shared-file awareness:** The only coordination rule: shared files (STATE.md, learnings.md, todo.md, CLAUDE.md) are edited from one terminal at a time. Check whether another terminal might be mid-edit before writing."
WHY: positive form names the rule (one terminal at a time) and the check (look before writing)

### directives/data-compliance.md

OLD: "1. **DPIA check.** If the feature handles personal data and no DPIA (Article 35) has been completed, STOP. A DPIA must be completed and approved before any personal data enters the system. This is non-negotiable. The DPIA documents: what data is collected, why, the legal basis, risks, and mitigations."
NEW: "1. **DPIA check.** Complete and approve a DPIA (Article 35) before any personal data enters the system. The DPIA documents: what data is collected, why, the legal basis, risks, and mitigations. This is the gate -- no DPIA, no data."
WHY: positive form names the action (complete and approve before any data) and the gate framing

OLD: "7. **Audit trail.** All access to and modifications of personal data must be logged (who, what, when). This log itself is retained for compliance purposes but must not contain the personal data it references (use IDs, not names)."
NEW: "7. **Audit trail.** Log all access to and modifications of personal data (who, what, when). The audit log references personal data by ID only -- the log row holds operation metadata, the data lives in its own table."
WHY: positive form names the rule (reference by ID) and the architecture (separate operation metadata from data)

OLD: "8. **Data minimisation.** Only collect what is needed for the stated purpose. If a feature can work without a field, don't collect it. If a field is only needed temporarily, delete it after use."
NEW: "8. **Data minimisation.** Collect only what is needed for the stated purpose. When a feature can work without a field, omit it. When a field is needed only temporarily, delete it after use."
WHY: positive form leads with the action (collect only / omit when optional)

OLD: "12. **Update privacy notice.** If a privacy notice exists, update it to accurately describe the new data processing. Reform UK's problem was that their privacy notice described data merging but they denied doing it. Never let the privacy notice diverge from reality."
NEW: "12. **Update privacy notice.** Update the privacy notice in the same PR that introduces the data processing it describes. Reform UK's problem was a privacy notice that described data merging while the company denied doing it -- the cure is co-located edits, not parallel docs."
WHY: positive form names the workflow (update in the same PR) and explains why with the case study

OLD: "Reform UK is being sued by 51 voters (High Court, Article 80(1) UK GDPR) for: (a) failing to respond to 96% of DSARs within the legal 30-day deadline, (b) sending false \"no record\" replies, (c) processing political opinion data without clear legal basis, (d) merging electoral register with third-party data without transparency. This is the first Article 80(1) case in the UK. Projects must not repeat these failures. Every feature that touches personal data must have automated SAR handling, transparent privacy notices, and documented legal bases."
NEW: "Reform UK is being sued by 51 voters (High Court, Article 80(1) UK GDPR) for: (a) failing to respond to 96% of DSARs within the legal 30-day deadline, (b) sending false \"no record\" replies, (c) processing political opinion data without clear legal basis, (d) merging electoral register with third-party data without transparency. This is the first Article 80(1) case in the UK. Every feature that touches personal data ships with automated SAR handling, a transparent privacy notice, and documented legal bases for each data category."
WHY: positive form leads with the requirement (every feature ships with X) instead of the prohibition (must not repeat failures)

### directives/data-safety.md

OLD: "- **Never copy `.env` between machines** -- regenerate credentials per environment."
NEW: "- **Each machine generates its own `.env`** -- regenerate credentials per environment rather than copying between hosts."
WHY: positive form names the rule (each machine generates) and the why (regenerate vs copy)

OLD: "- **Never use production connection strings in any `.env` file** -- even \"just for testing.\" There is no \"just for testing\" with regulated data."
NEW: "- **Production connection strings live only on production hosts.** Local `.env` files point at non-prod databases. There is no \"just for testing\" with regulated data."
WHY: positive form names the rule (production strings live on production) instead of the prohibition

OLD: "| `DROP DATABASE` | Irrecoverable total loss | Never. There is no safe alternative to dropping a production database. |"
NEW: "| `DROP DATABASE` | Irrecoverable total loss | Halt and escalate to user. There is no safe alternative -- dropping a production database is never an automated action. |"
WHY: positive form names the action (halt and escalate) — the table column reads as a runbook step instead of a denial

OLD: "| `supabase db reset` | Wipes entire database, replays migrations from scratch | Never run against production. Use only on local/branch databases. |"
NEW: "| `supabase db reset` | Wipes entire database, replays migrations from scratch | Use only on local or branch databases. Confirm `supabase status` shows a non-production link before running. |"
WHY: positive form names the use (local/branch only) and the verification (check status first)

OLD: "4. **One migration at a time.** Never batch multiple migrations. If one fails, you need to know which one."
NEW: "4. **One migration at a time.** Run each migration on its own; on failure, the offending migration is the most recent run. Batching obscures which migration broke."
WHY: positive form names the rule (one at a time) and the why (failure attribution)

### directives/delivery-rules.md

OLD: "- **YOU MUST NOT overwrite or delete existing directives without explicit permission.** Propose changes, don't make them. New directives: also add a trigger to CLAUDE.md Progressive Disclosure."
NEW: "- **Directive changes go through `/sync-doe` with user approval.** Propose; the user merges. New directives also add a trigger to CLAUDE.md Progressive Disclosure."
WHY: positive form names the workflow (`/sync-doe` + user merges) and the trigger registration step

OLD: "- **YOU MUST NOT store secrets outside `.env`.** No API keys in code, comments, or logs."
NEW: "- **Secrets live in `.env` only.** Code reads them via the documented loader; nothing else (commit messages, comments, logs) holds a live secret."
WHY: positive form names the rule (live in .env, read via loader)

OLD: "- **YOU MUST NOT edit `~/doe-starter-kit` directly.** All changes go through `/sync-doe` (versioning, tagging, releases)."
NEW: "- **Kit edits go through `/sync-doe`.** Versioning, tagging, and release happen automatically; the `guard_kit_writes` PreToolUse hook blocks direct writes."
WHY: positive form names the workflow (`/sync-doe`) and the enforcement mechanism (hook)

OLD: "- **YOU MUST NOT force-push, revert commits, or delete branches without explicit permission.** If something needs rolling back, show what you want to revert and why first."
NEW: "- **Destructive git operations require explicit user approval.** Force-push, revert, branch delete: show the diff and ask before acting."
WHY: positive form names the gate (explicit approval) and the workflow (show + ask)

OLD: "- **When a hook blocks an action, fix it immediately.** Show what was flagged, what you fixed, and proof. Don't just say \"I updated the file.\""
NEW: "- **When a hook blocks an action, fix the underlying issue and report back with evidence.** Show what was flagged, what changed, and the verification (re-run, grep, test pass)."
WHY: positive form names the action (fix + report with evidence) and the contents of the report

OLD: "- **Wave agents MUST NOT edit shared files on main** (`todo.md`, `CLAUDE.md`, `learnings.md`, `STATE.md`). Coordinator fixes after `--merge`."
NEW: "- **Wave agents edit only files in their `Owns:` declaration.** Shared files (`todo.md`, `CLAUDE.md`, `learnings.md`, `STATE.md`) are written by the coordinator after `--merge`."
WHY: positive form names the scope (own files) and the responsibility for shared files (coordinator post-merge)

### directives/documentation-governance.md

OLD: "- **learnings.md:** Already has its own update rules via Rule 8. The front-matter addition is new — add it on next edit, don't force a separate commit just for front-matter."
NEW: "- **learnings.md:** Already has its own update rules via Rule 8. The front-matter addition is new — add it on the next edit to learnings.md so the front-matter and content land in the same commit."
WHY: positive form names the action (add on next edit, co-located)

### directives/framework-evolution.md


(Clean per audit — no negation patterns.)

### directives/git-conventions.md

OLD: "## Migration from legacy `vX.Y.Z:` format\n\nPre-v1.57.0 kit history used `vX.Y.Z: <description>` for release commits. The allowlist preserves these unchanged in `git log`. New commits should use `chore(release): vX.Y.Z`. Don't rewrite history to backfill — the `git log` is an honest record of how the convention evolved, and rewriting would invalidate every downstream clone."
NEW: "## Migration from legacy `vX.Y.Z:` format\n\nPre-v1.57.0 kit history used `vX.Y.Z: <description>` for release commits. The allowlist preserves these unchanged in `git log`. New commits use `chore(release): vX.Y.Z`. Old commits stay in their original format -- the `git log` is the audit record of how the convention evolved, and rewriting would invalidate every downstream clone."
WHY: positive form names the rule (new commits use CC; old commits stay) instead of the prohibition (don't rewrite)

### directives/incident-response.md

OLD: "3. Do NOT destroy evidence. Preserve logs and database state."
NEW: "3. Preserve all logs and database state. Snapshot the affected systems before any remediation -- the snapshot is the evidence record."
WHY: positive form names the action (preserve + snapshot) and the reason (evidence record)

OLD: "- Do NOT send a blanket \"no records found\" response without actually checking. This is what Reform UK did and it is now evidence against them."
NEW: "- Verify the database for the data subject before responding. A \"no records\" reply requires evidence -- Reform UK sent blanket replies and those replies are now evidence against them."
WHY: positive form names the action (verify before responding, evidence-backed reply) and keeps the case study as a warning

OLD: "- **Electoral register breach** is both a GDPR matter (ICO) and a criminal matter (police). Take legal advice before responding. Do not self-incriminate."
NEW: "- **Electoral register breach** is both a GDPR matter (ICO) and a criminal matter (police). Take legal advice before any external statement; the lawyer drafts the wording."
WHY: positive form names the workflow (legal advice → lawyer drafts) and removes the self-incriminate prohibition

OLD: "- **DSAR from a hostile party** (e.g. political opponent fishing for internal data) -- you must still comply. DSARs cannot be refused based on the requestor's motives, but you can refuse if the request is \"manifestly unfounded or excessive\" (Article 12(5)). Document the reasoning carefully."
NEW: "- **DSAR from a hostile party** (e.g. political opponent fishing for internal data) -- you still comply. DSARs are processed regardless of the requestor's motives, except where \"manifestly unfounded or excessive\" applies (Article 12(5)). Document the reasoning carefully when invoking the exception."
WHY: positive form names the rule (process regardless of motive) and the narrow exception (Article 12(5))

### directives/integrations.md

OLD: "Add the URLs to `.env` (gitignored, never committed):"
NEW: "Add the URLs to `.env` -- gitignored, with the pre-commit secret-detection hook as a backstop against accidental commits:"
WHY: positive form names the rule (gitignored + hook as backstop) and removes the never-committed clause

OLD: "- Webhook URLs are secrets. Store in `.env`, never in committed files. Pre-commit hooks with secret detection will catch accidental commits."
NEW: "- Webhook URLs are secrets and live in `.env`. The pre-commit secret-detection hook gates accidental commits to other files."
WHY: positive form names the location (.env) and the gate (secret-detection hook)

### directives/kit-development.md

OLD: "The conventions specific to contributing changes back to the DOE Starter Kit itself — versioning model, branch naming, CHANGELOG structure, release mechanics, CC self-dogfood, the tests-included-by-default expectation, and the hook-design rule. These are kit-only rules; they don't apply to projects that consume the kit."
NEW: "The conventions specific to contributing changes back to the DOE Starter Kit itself — versioning model, branch naming, CHANGELOG structure, release mechanics, CC self-dogfood, the tests-included-by-default expectation, and the hook-design rule. These rules apply to kit work only; projects that consume the kit are governed by their own CLAUDE.md."
WHY: positive form names the scope (kit work only; projects use their own CLAUDE.md)

OLD: "The release notes come from the CHANGELOG hero + sections of the release being shipped. Don't hand-write release notes; the CHANGELOG is the source of truth."
NEW: "The release notes come from the CHANGELOG hero + sections of the release being shipped. The CHANGELOG is the source of truth -- write the entry there first; `gh release create` lifts it."
WHY: positive form names the workflow (write in CHANGELOG; gh release lifts it)

### directives/manual-testing.md


(Clean per audit — no negation patterns.)

### directives/planning-rules.md

OLD: "### Ask, don't assume\nAmbiguous requirement? Ask. Separate research and implementation into different sessions -- context pollution hurts both. If you're not sure what the user means, clarify before spending tokens on the wrong path."
NEW: "### Ask when ambiguous\nMatch the question to the smallest decision that unblocks you. Separate research from implementation sessions -- context pollution hurts both. When the user's meaning is unclear, clarify before spending tokens on the wrong path."
WHY: positive form names the trigger (when ambiguous) and the action (smallest unblocking question)

OLD: "- Continue building autonomously -- do NOT stop per-step for manual approval."
NEW: "- Continue building autonomously. Manual approval batches at feature end (or at the mid-feature gate for 5+ step features)."
WHY: positive form names the action (continue + batch at feature end) instead of the prohibition (don't stop)

OLD: "### Informal Parallel\n2-3 terminals on independent tasks. Shared-file awareness: don't edit STATE.md, learnings.md, todo.md, or CLAUDE.md from multiple terminals simultaneously."
NEW: "### Informal Parallel\n2-3 terminals on independent tasks. Shared-file awareness: edit shared files (STATE.md, learnings.md, todo.md, CLAUDE.md) from one terminal at a time. Check before writing in multi-terminal mode."
WHY: positive form names the rule (one terminal at a time + check before writing)

### directives/rationalisation-tables.md

OLD: "**If your current thought matches any excuse in any table below: stop. Do what the \"What to do instead\" column says. No exceptions.**"
NEW: "**When your current thought matches any excuse in any table below, follow the \"What to do instead\" column. The Iron Law (below) defines the only legitimate skip path.**"
WHY: positive form names the action (follow the column) and points to the legitimate skip path

### directives/security-rules.md

OLD: "3. **Fail closed.** If a security check errors, deny access. Never default to permissive."
NEW: "3. **Fail closed.** Security checks deny access on error -- the default response is denial, not permission."
WHY: positive form names the rule (deny on error; default = denial)

OLD: "- **no eval.** Never use `eval()`, `Function()`, `setTimeout(string)`, or any dynamic code execution. There are no exceptions."
NEW: "- **No dynamic execution.** Dynamic code execution lives in pre-registered, statically-analysable code paths. JavaScript: avoid the three string-execution APIs (`eval()`, `Function()` constructor, string form of `setTimeout`/`setInterval`) -- pass functions instead."
WHY: positive form names the rule (statically-analysable paths only) and the JS-specific guidance (pass functions)

OLD: "- **Use parameterised queries.** Never concatenate user input into SQL, NoSQL, or ORM queries. Use parameterised / prepared statements exclusively."
NEW: "- **Use parameterised queries.** All SQL, NoSQL, and ORM queries take user input via parameter bindings -- the query string is a static template, the values pass separately."
WHY: positive form names the rule (parameter bindings; static template + separate values)

OLD: "- **Sanitise all dynamic content.** Use `escapeHTML` or the framework's built-in escaping for every value inserted into the DOM. Never insert untrusted content via `innerHTML`, `dangerouslySetInnerHTML`, `v-html`, or `[innerHTML]` without sanitisation."
NEW: "- **Sanitise all dynamic content.** DOM insertion of untrusted values goes through `textContent` or framework escaping (Vue/React/Angular built-ins). `innerHTML`, `dangerouslySetInnerHTML`, `v-html`, and `[innerHTML]` are reserved for known-safe HTML you generated yourself."
WHY: positive form names the safe APIs (textContent, framework escaping) and the narrow scope where innerHTML is acceptable

OLD: "- **Never log secrets.** API keys, tokens, passwords, and session identifiers must never appear in logs, error messages, or client-side output."
NEW: "- **Redact secrets at the log boundary.** A logging redactor strips secret-shaped tokens (API keys, tokens, passwords, session identifiers) before any sink receives the log line."
WHY: positive form names the architecture (redactor at log boundary)

OLD: "- **Classify data in logging.** Mark sensitive fields as `CONFIDENTIAL` or `RESTRICTED` in log schemas. Never log fields at a classification level higher than the log sink's clearance."
NEW: "- **Classify data in logging.** Mark sensitive fields as `CONFIDENTIAL` or `RESTRICTED` in log schemas. Routing matches sink clearance to field classification -- a CONFIDENTIAL field only flows to a CONFIDENTIAL-cleared sink."
WHY: positive form names the architecture (clearance-matched routing)

OLD: "- **Environment variables for secrets.** Store credentials in `.env` or a secrets manager. Never commit secrets to version control."
NEW: "- **Environment variables for secrets.** Credentials live in `.env` or a secrets manager. The pre-commit secret-detection hook gates accidental commits to other files."
WHY: positive form names the storage location and the gate (pre-commit hook)

OLD: "- **Use established libraries.** Do not implement authentication, hashing, or token generation from scratch."
NEW: "- **Use established libraries.** Authentication, password hashing, and token generation come from established libraries -- Argon2 / libsodium / the framework's auth module -- never a from-scratch implementation."
WHY: positive form names the libraries (Argon2, libsodium, framework auth)

### directives/self-annealing.md


(Clean per audit — no negation patterns.)

### directives/serial-dispatch-protocol.md

OLD: "- **Hotfixes:** If something is broken in production, fix it directly. Don't ceremony a one-line fix."
NEW: "- **Hotfixes:** When something is broken in production, fix it directly -- skip the dispatch ceremony for one-line fixes."
WHY: positive form names the action (fix directly + skip ceremony)

OLD: "- If mid-feature you discover the plan is wrong, stop dispatch and update the plan first -- don't let implementers build on a broken foundation"
NEW: "- When mid-feature you discover the plan is wrong, halt dispatch and update the plan before re-dispatching -- implementers build on the corrected foundation"
WHY: positive form names the workflow (halt + update plan + re-dispatch on corrected foundation)

### directives/starter-kit-pull.md

OLD: "If there are local uncommitted changes, stop and ask the user how to handle them before proceeding."
NEW: "When there are local uncommitted changes, pause and ask the user how to handle them before proceeding."
WHY: positive form names the action (pause + ask) instead of the prohibition (stop)

OLD: "IMPORTANT: Projects may have added their own hooks or settings entries. Never remove project-specific additions — only add/update kit-provided content."
NEW: "IMPORTANT: Projects may have added their own hooks or settings entries. Project-specific additions are preserved; the merge is additive -- only kit-provided content is added or updated."
WHY: positive form names the rule (preserved + additive merge)

OLD: "### Step 9: Templates (learnings.md, todo.md)\nCompare format rules only — never touch content.\n\n- **learnings.md** — if the kit added new section headings or format rules in the template, propose adding them to the project's learnings.md. Never modify existing learnings content.\n- **todo.md** — if the kit changed format rules (e.g. new step format, new status tags), propose the rule change. Never modify existing tasks."
NEW: "### Step 9: Templates (learnings.md, todo.md)\nCompare format rules only -- content is read-only during pull.\n\n- **learnings.md** — when the kit added new section headings or format rules in the template, propose adding them to the project's learnings.md. Existing learnings content is read-only during pull.\n- **todo.md** — when the kit changed format rules (e.g., new step format, new status tags), propose the rule change. Existing tasks are read-only during pull."
WHY: positive form names the rule (content read-only during pull)

OLD: "- Never touch `@register(\"project-specific\")` checks — these are project customizations"
NEW: "- `@register(\"project-specific\")` checks are preserved as-is during pull -- they are project customizations"
WHY: positive form names the rule (preserved as-is)

OLD: "- Make surgical edits (add/update specific sections)\n- Never replace files wholesale"
NEW: "- Make surgical edits (add/update specific sections)\n- Pull merges additively, line-by-line; wholesale replacement is reserved for files the project doesn't yet have"
WHY: positive form names the rule (additive merge; wholesale only for new files)

### directives/starter-kit-sync.md

OLD: "When in doubt, flag it for the user — don't silently skip."
NEW: "When in doubt, flag it for the user -- the universal/project-specific call is theirs to make."
WHY: positive form names the action (flag for user; their call) instead of the prohibition

OLD: "If there are local uncommitted changes, stop and ask the user how to handle them before proceeding."
NEW: "When there are local uncommitted changes, pause and ask the user how to handle them before proceeding."
WHY: positive form names the action (pause + ask) -- mirrors starter-kit-pull

OLD: "IMPORTANT: Never replace a file wholesale. Merge improvements additively — add new rules, update changed rules, but keep existing starter kit content that isn't present in the current project."
NEW: "IMPORTANT: Merge improvements additively -- add new rules, update changed rules, and keep existing starter-kit content that the current project doesn't have. Wholesale replacement is reserved for files the kit doesn't yet have."
WHY: positive form names the rule (additive merge; wholesale only for new files)

OLD: "**Run each of these as a SEPARATE Bash tool call** — do not `&&` chain them, and do not pipe-trim their output with `| head/tail -N` when chaining. Bash pipelines return the last command's exit code, so `git commit ... | tail -5 && git tag` fires the tag even if commit was blocked by a hook (`tail` exits 0). Separate calls make each exit code visible. If you must chain, prefix with `set -o pipefail`."
NEW: "**Run each of these as a SEPARATE Bash tool call** -- one command per call so each exit code propagates independently. Bash pipelines return the last command's exit code: `git commit ... | tail -5 && git tag` fires the tag even when commit was blocked by a hook (`tail` exits 0). Separate calls keep each exit code visible. When chaining is unavoidable, prefix with `set -o pipefail`."
WHY: positive form names the rule (one command per call) and the why (independent exit codes)

OLD: "- If git pull in Step 2 reveals conflicts, stop and show the user — do not auto-resolve"
NEW: "- When git pull in Step 2 reveals conflicts, surface the conflict to the user before any merge action -- conflicts are the user's call to resolve"
WHY: positive form names the action (surface to user; user resolves)

### directives/subagent-protocol.md

OLD: "- **Coordinator action:** Provide the missing context and re-dispatch the same agent. Do NOT guess the answers — if the coordinator doesn't know either, escalate to the user."
NEW: "- **Coordinator action:** Provide the missing context and re-dispatch the same agent. When the coordinator lacks the answer, escalate to the user -- the user holds the ground truth."
WHY: positive form names the action (escalate to user) and the why (user holds ground truth)

OLD: "- If a subagent reports BLOCKED, do not blindly re-dispatch — the blocker must be resolved first"
NEW: "- When a subagent reports BLOCKED, resolve the blocker before re-dispatching -- the same dispatch on the same blocker produces the same BLOCKED"
WHY: positive form names the action (resolve blocker first) and the why (same input → same output)

### directives/testing-strategy.md

OLD: "1. **Update an existing test** -- if your change adjusts the contract of a function that already has coverage, update the assertions in the matching test file. Don't delete the test \"because it's broken now\" -- the test was the spec."
NEW: "1. **Update an existing test** -- when your change adjusts the contract of a function that already has coverage, update the assertions in the matching test file. The test stays as the spec; deleting it because it failed makes the spec disappear."
WHY: positive form names the action (update assertions) and the why (test = spec)

OLD: "When a new auto tool ships (e.g. Playwright MCP, a new linter, a new methodology scenario), don't build an auto-promotion engine -- review existing `[manual]` contracts by hand and promote the ones the new tool can verify."
NEW: "When a new auto tool ships (e.g., Playwright MCP, a new linter, a new methodology scenario), review existing `[manual]` contracts by hand and promote the ones the new tool can verify -- the human-curated promotion catches edge cases an auto-promotion engine would miss."
WHY: positive form names the action (review by hand + promote) and the why (catches edge cases)

## Tier 3 — kit's learnings.md + remaining files

(Populated during Step 5 of v1.59.0.)

## Behavioural changes

### `.claude/settings.json` — PreToolUse + PostToolUse matcher casing fix (Tier 2 work, lands Step 4)

Scope: PreToolUse and PostToolUse hook matchers across the kit's `.claude/settings.json`.
Old behaviour: matchers were declared in lowercase piped form (`write|create|edit`, `bash`, `read`) which does NOT match Claude Code's actual Tool name strings. Result: every PreToolUse and PostToolUse hook in the kit has been silently non-functional — the scripts were registered but never invoked. Confirmed empirically during Step 3 by observing that Anthropic's official `security-guidance` plugin (matcher `Edit|Write|MultiEdit`) DID fire while kit hooks (matcher `write|create|edit`) did NOT, despite manual stdin tests proving the kit hook scripts work when invoked.
New behaviour: matchers use case-sensitive Tool name strings — `Edit|Write|MultiEdit`, `Bash`, `Read`. After the fix, six PreToolUse hooks (`protect_directives`, `block_secrets_in_code`, `guard_kit_writes`, `block_dangerous_commands`, `enforce_review_gate`, `confirm_pr_merge`) and three PostToolUse hooks (`copy_plan_to_project`, `check_completed_feature`, `check_plan_freshness_hook`) start firing as designed.
Pull-impact for projects on v1.58.0 or earlier: their kit hooks have likewise been non-functional. After pulling v1.59.0, projects will newly observe the rules these hooks enforce: kit-write blocks (`/sync-doe` is now actually mandatory, not just documented), secret-detection blocks on Edit/Write, plan-copy automation, completed-feature warnings, plan-freshness reads. None of these change rule semantics — they enforce existing rules that were silently bypassed.
Pull-impact grep: `grep -E '"matcher": "(write|create|read|bash|edit)[|"]' .claude/settings.json` -- the trailing `[|"]` character class accepts either `|` (mid-pipe-list, as in `"write|create|edit"`) or `"` (end-of-list, as in `"bash"`), so single-token AND pipe-separated lowercase matchers both match. If a project has lowercase matchers, the v1.59.0 pull updates them. If a project has its own additional hooks with lowercase matchers, flag for manual review.

### `.claude/settings.json` — `permissions.deny` block

Scope: tool-layer permission (Read).
Old behaviour: nothing prevented Claude Code from invoking Read on `.env` or `.env.*` files; the existing `block_secrets_in_code.py` PreToolUse hook only gated writes.
New behaviour: Read is denied at the permission layer for `.env`, `./.env`, `./**/.env`, `.env.*`, and `./**/.env.*`. The model receives a permission-denied response before the file is opened.
Pull-impact grep: `grep -rnE 'Read\((\.env|.*/.env)' .` — if a project's docs or scripts reference reading .env via Claude tools, those references should be removed or updated.

### `protect_directives.py` — extended to Bash matcher (Tier 2 work, lands Step 4)

Scope: PreToolUse, previously `write|create|edit` matcher only; now also `Bash`.
Old behaviour: writes via `Bash(cat > directives/X.md)`, `sed -i directives/X.md`, etc., bypass the hook.
New behaviour: hook also runs on the `Bash` matcher and parses for redirected writes / in-place edits to `directives/`.
Pull-impact grep: `grep -rnE '(sed -i|cat *>|tee *>) .*directives/|>.* directives/' execution/ tasks/ .githooks/` — flag any project workflow that edits directives via Bash so it can be migrated to Edit/Write before pulling.

### `block_secrets_in_code.py` — extended to Bash matcher (Tier 2 work, lands Step 4)

Scope: PreToolUse, previously `write|create|edit` matcher only; now also `Bash`.
Old behaviour: writes via `Bash(cat > path/with/secret …)` or `echo SECRET=… >> .env.local` bypass the hook.
New behaviour: hook also runs on the `Bash` matcher and parses for redirected writes (`>`, `>>`, `tee`, `cat >`, `cat >>`) that contain the secret pattern lexicon (`sk-`, `ghp_`, JWTs, etc.). Also blocks any redirection targeting an `.env.*` variant regardless of content. Out of scope (intentional): write paths that don't surface a literal redirect token at the Bash layer (`python3 -c "open('f','w').write(...)"`, `dd of=config`, application-level writes via `subprocess.run` argv lists). The hook is one defence layer among several; CI scanning and code review cover the rest.
Pull-impact grep: `grep -rnE '(echo|printf|cat *>|tee).*-?(key|token|secret)' execution/ scripts/` — flag any project script that pipes secrets into files via Bash. The alternation is parenthesised so the verb anchor (`echo|printf|cat >|tee`) and the path-prefix `.*` apply to all three suffixes (`key`, `token`, `secret`); the previous unparenthesised form `.*-?key|-?token|-?secret` had ERE precedence that bound the verb only to the `key` branch and matched `token`/`secret` everywhere.

## Customised-directive check

Run from the project root before pulling:

```bash
PINNED_TAG=$(grep -oE 'kit v?[0-9]+\.[0-9]+\.[0-9]+' STATE.md | head -1 | grep -oE '[0-9]+\.[0-9]+\.[0-9]+')
KIT_DIR=~/doe-starter-kit
for f in directives/*.md; do
    diff -q "$f" "$KIT_DIR/$f" 2>/dev/null \
        || echo "PROJECT-CUSTOMISED (3-way merge needed): $f"
done
```

If any directive is project-customised, the v1.59.0 pull will require a manual 3-way merge for that file (project-edits + kit-edits-since-pin + kit v1.59.0 rewrite).
